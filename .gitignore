.env
channels.json
__pycache__/
*.pyc
*.pyo
.DS_Store
.venv/

# seen_jobs.json must NOT be gitignored — GitHub Actions commits it back to the repo
# to persist the deduplication state between runs.

# Local-only working files, never committed
*.local
# Finder duplicate copies ("jev 2.py")
* 2.py
* 2.md
